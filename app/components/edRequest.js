import React from 'react';
import { Button, Form, FormGroup, Label, Input, FormText } from 'reactstrap';
import API from "../utils/API";


export default class edRequest extends React.Component {
    

    render(){
        return(
            <h2>This is the EdRequest Form</h2>
        )
    }
}